<?php
require 'db.php';
echo "PDO Driver: " . $pdo->getAttribute(PDO::ATTR_DRIVER_NAME) . "\n";

try {
    $stmt = $pdo->query("SELECT * FROM revision_plans LIMIT 1");
    echo "Success querying revision_plans!\n";
    var_dump($stmt->fetchAll());
} catch (Exception $e) {
    echo "Error querying revision_plans: " . $e->getMessage() . "\n";
}
